---
description: 项目边界与操作规则——修改仅限项目目录内，并按操作规则执行
alwaysApply: true
---

# 项目边界与操作规则

## 边界（不得越界）

- **所有修改仅允许发生在当前项目目录内。**
- 项目目录 = 当前 Cursor 工作区根目录及其全部子目录（即包含 `llm_backend`、`frontend`、`docs` 等的根路径及其子路径）。
- **禁止**对上述范围以外的任何路径进行写入、编辑或删除（不修改系统配置、其他项目路径、Cursor 全局设置等）。
- 新建文件时，目标路径必须位于项目目录内；若任务涉及项目外或全局配置，只提供说明或步骤，不直接修改项目外文件。

## 每次修改须遵守

1. **先确认范围**：执行修改前确认所涉文件均在项目目录内，否则不执行越界操作。
2. **符合项目约定**：风格、命名、结构尽量与现有代码和文档一致；不违背 `docs/操作规则.md` 及 `design.md` 等已有约定；若有偏离须在修改处或文档中说明。
3. **可追溯**：对行为或接口有影响的修改，视情况在关键处加注释或更新相关文档（如 `design.md`），便于维护与对照。

## 参考

- 完整说明见项目内 `docs/操作规则.md`。
- 边界规则为硬性约束；通用规则为默认准则，例外时须注明原因。
